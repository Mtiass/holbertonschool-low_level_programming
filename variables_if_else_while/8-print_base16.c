#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */
int main(void)
{
	char bas16;

	for (bas16 = 48; bas16 <= 57; bas16++)
	{
		putchar(bas16);
	}
	for (bas16 = 97; bas16 = 102; bas16++)
	{
		putchar(bas16);
	}
	putchar('\n');
	return (0);
}
