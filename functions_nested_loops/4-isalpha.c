#include "main.h"

/**
 * _isalpha - Entry point
 * Description: checks if
 * lowercase ir uppercase letter.
 * @c: char to be checked
 * Return: 1
 */
int _isalpha(int c)
{
	_isalpha(c);
	return (1);
}
