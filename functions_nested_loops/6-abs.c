#include "main.h"
/**
 * _abs - Entry point
 *
 * Description:" computes the absolutei
 * value of an integer."
 *
 * @A: variable -t int
 * Return: A.
 */
int _abs(int A)
{
	if (A < 0)
	{
		return (A * -1);
	}
	else
	{
		return (A);
	}
}
