#include "main.h"
/**
 * _abs - Entry point
 *
 * Description:" computes the absolutei
 * value of an integer."
 *
 * @A: variable -t int
 * Return: 
 */
int _abs(int A)
{
	unsigned int UA;

	UA = (unsigned int)A;
	_putchar(UA);
	return (UA);
}
