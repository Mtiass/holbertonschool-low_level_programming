#include "main.h"
/**
 * _abs - Entry point
 *
 * Description:" computes the absolutei
 * value of an integer."
 *
 * @A: variable -t int
 * Return: UA.
 */
int _abs(int A)
{
	int UA;

	UA = (unsigned int A);
	return (UA);
}
