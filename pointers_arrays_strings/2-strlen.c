#include "main.h"

/**
 * strlen - Entry point
 * Description:"In README.md"
 * @s:"var type char."
 * Return: int
 */
int _strlen(char *s)
{
	int stle;

	while (s[] != '\0')
	{
		s[0] = stle;
		stle++;
	}
	return (stle);
}
