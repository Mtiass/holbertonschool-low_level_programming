#include "main.h"

/**
 * strlen - Entry point
 * Description:"In README.md"
 * @s:"var type char."
 * Return: int
 */
int _strlen(char *s)
{
	char z;

	for (z = 0; z < *s; z++)
	{
		return (z + '0');
	}
}
