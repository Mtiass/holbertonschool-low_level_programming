#include "main.h"

/**
 * strlen - Entry point
 * Description:"In README.md"
 * @s:"var type char."
 * Return: int
 */
int _strlen(char *s)
{
	int stle;

	for (stle = 0; stle < [*s]; stle++);
	return (stle);
}
